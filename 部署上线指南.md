# Recall 错题本 · 部署上线指南（模块 5）

本指南对应课程模块 5「部署上线与产品发布」，把原方案中的**域名申请**与**部署方案**落到 Recall 项目的可执行步骤。

---

## 0. 先看清项目结构（决定怎么部署）

Recall 不是普通的「纯静态前端」，而是一个**前后端一体单体**：

- 后端 `backend/` 是 Python + FastAPI（uvicorn 默认监听 **8000**）。
- `backend/app/main.py` 在生产环境下会**自己挂载 `frontend/dist`**，一个进程同时提供：
  - 前端页面（`/`）
  - 后端接口（`/api/...`）
- 后端依赖**极轻**：默认 `MOCK_EXTERNAL=true`，PaddleOCR / ChromaDB / WeasyPrint 等重依赖都在 `requirements.txt` 里**注释关闭**，无需外部 GPU/AI 服务即可完整演示。
- 数据用 **SQLite** 文件（`backend/data/`），本地靠 `docker-compose.yml` 的 volume 持久化。

### 这决定了两件事
1. **EdgeOne Pages 不能单独跑后端**：EdgeOne Pages 只托管静态前端，原生跑不了 Python FastAPI。所以原表格里「EdgeOne 一体化托管 FastAPI」对 Recall **不成立**——必须让后端落到能跑 Python 的地方（阿里云 FC）。
2. **最省事的形态 = 单体部署**：直接把「前端 dist + FastAPI」整体当一个服务部署（一个函数 = 页面 + 接口，同域无跨域）。

---

## 1. 方案选择（课程两种方案如何落地）

| 方案 | 原表格说法 | Recall 实际落地 | 推荐度 |
|---|---|---|---|
| 阶段一：一体化 | EdgeOne 托管前后端 | ❌ EdgeOne 跑不了 Python | 不适用 |
| **阶段一（替代）：FC 容器一体化** | — | 阿里云 FC 跑整个 Docker 镜像（前端+后端一体） | ⭐⭐⭐ 最推荐 |
| 阶段二：分离 | EdgeOne 前端 + 阿里云 FC 后端 | EdgeOne Pages 托管 dist + FC 跑 FastAPI（开 CORS / 反向代理） | ⭐⭐ 产业级规范 |

> 结论：**课程 MVP 快速验证用「FC 容器一体化」**；**最终项目 / 想体现大厂规范用「前后端分离」**。两者都免费或接近免费（见下文）。

---

## 2. 方案一（推荐）：阿里云 FC 容器一体化部署

把现有 `backend/Dockerfile`（已含「构建前端 dist → 运行 FastAPI 并托管 dist」）打成镜像，交给阿里云函数计算（FC）运行。**一个函数 = 前端页面 + `/api` 接口，同域无跨域，最契合本项目。**

### 前置条件
- 阿里云账号（完成实名认证）。
- 开通 **函数计算 FC** 与 **容器镜像服务 ACR（个人版/企业版）**。
- 本地安装 Docker（用于构建并推送镜像）。

### 步骤
1. **本地构建镜像**（在仓库根目录，Dockerfile 上下文已正确配置）：
   ```bash
   # 登录 ACR（在 ACR 控制台获取临时登录指令）
   docker login --username=你的阿里云账号 registry.cn-hangzhou.aliyuncs.com
   docker build -t registry.cn-hangzhou.aliyuncs.com/<你的命名空间>/recall:latest .
   docker push registry.cn-hangzhou.aliyuncs.com/<你的命名空间>/recall:latest
   ```
   > 镜像约几百 MB（含 Node 构建前端 + Python 运行时），首次推送稍慢，之后 FC 拉取即可。

2. **FC 创建函数（容器镜像）**：
   - 函数计算控制台 → 创建函数 → 选择「**使用容器镜像**」。
   - 镜像地址填上面 push 的地址。
   - **监听端口**：填 `8000`（与 Dockerfile 中 `uvicorn --port 8000` 一致；若想用 FC 默认 9000，则需把 Dockerfile 启动命令端口改为 9000）。
   - 运行环境内存建议 ≥ 512 MB（构建前端时已装依赖，运行期轻量）。

3. **环境变量**（FC 函数配置 → 环境变量）：
   ```
   MOCK_EXTERNAL=true
   ```
   （保持 Mock 模式，演示无需 DeepSeek 真实 key；若要真 AI，再填 `DEEPSEEK_API_KEY` 并把 `MOCK_EXTERNAL` 设为 false。）

4. **访问**：FC 会分配一个公网地址（如 `https://xxx.cn-hangzhou.fc.aliyuncs.com`），打开即是 Recall 错题本完整页面。可在 FC 控制台绑定自定义域名（见第 4 节）。

> 费用：FC 新用户有**连续 3 个月免费额度（每月 100 万次调用）**，课程周期内完全够用。

---

## 3. 方案二：前后端分离（EdgeOne Pages 前端 + 阿里云 FC 后端）

更符合「前后端独立扩展、资源隔离」的产业级规范。

### 3.1 前端 → EdgeOne Pages（免费、国内免备案）
EdgeOne Pages 托管 `frontend/dist` 纯静态资源：
- 构建命令：`npm run build`
- 输出目录：`dist`
- 在 EdgeOne Pages 控制台关联 Git 仓库（选 `frontend/` 子目录）或上传 dist 压缩包。
- 默认分配 `xxx.edgeone.app` 子域名，**国内可直接访问、免备案**。

**让前端知道后端地址（两种方式任选其一）：**
- **方式 A（推荐，零跨域）— EdgeOne 反向代理**：在 EdgeOne Pages 控制台配置「边缘规则 / 重写 / 代理」，把 `/api/*` 反向代理到 FC 后端域名。前端代码**无需改动**（仍用同源 `/api`）。
- **方式 B — 环境变量指定后端**：构建时设置 `VITE_API_BASE=https://<你的FC域名>`，前端会自动指向该后端（本项目 `frontend/src/api/http.ts` 已支持 `VITE_API_BASE`，本地无该变量时自动回退 `/api`，不影响开发）。

### 3.2 后端 → 阿里云 FC（FastAPI）

#### 方式一：容器镜像（同方案一）
按第 2 节的镜像方式部署即可。

#### 方式二（最省事·零成本）：代码包直传，免 Docker / 免 ACR ⭐
不想装 Docker、不开 ACR 的首选。只用 FC 一个服务，免费额度内基本不花钱。

**① 本地打包（在你自己电脑执行，约 1 分钟）**
项目已提供一键脚本 `deploy/build_fc_package.py`（会自动构建前端、把 dist 嵌进 backend、打成 zip）：
```bash
cd Recall错题本
python deploy/build_fc_package.py
```
生成的 `recall-fc.zip` 内含 `app/`、`requirements.txt`、`frontend/dist/`。

**② FC 控制台创建函数（逐步）**
1. 函数计算 FC 控制台 → **创建函数** → 运行环境选 **Python 3.12**（从零开始 / 自定义运行时均可）。
2. **上传代码**：选「**上传 zip 包**」，选择 `recall-fc.zip`。
3. **启动命令**填：
   ```
   python -m uvicorn app.main:app --host 0.0.0.0 --port 9000
   ```
4. **监听端口**：`9000`（Python 运行时默认）。
5. **自动安装依赖**：在高级设置勾选「**使用控制台临时安装依赖**」，或在函数详情里点「**在线安装依赖**」，FC 会按包内 `requirements.txt` 自动安装 fastapi / uvicorn / sqlalchemy 等（无需你本地预装）。
6. **环境变量**（关键——FC 代码目录只读，必须把数据指到 /tmp 才能写入）：
   ```
   MOCK_EXTERNAL=true
   RECALL_DATA_DIR=/tmp/recall-data
   RECALL_DB_PATH=/tmp/recall-data/recall.db
   RECALL_UPLOAD_DIR=/tmp/recall-uploads
   ```
7. **内存**：≥ 256 MB（运行期轻量，256 足够；冷启动慢可调 512）。
8. 保存并**部署**。

**③ 访问**：FC 分配公网地址（如 `https://xxx.cn-hangzhou.fc.aliyuncs.com`），打开即完整 Recall 错题本。

**CORS（仅分离部署「方式 B」需要）**：若前端用方式 B 直接跨域访问 FC，需把 EdgeOne 域名加入 `backend/app/main.py` 的 `allow_origins`（或设环境变量 `CORS_ORIGINS`）；若前端用方式 A 反向代理或本方案单体托管，则无需。

> 费用：FC 按量付费 + 长期免费额度（每月百万级调用、固定免费算力），课程演示量基本 **0 元**；ACR 完全不用开。

---

## 4. 域名申请与国内访问配置

| 类型 | 示例 | 特点 | 备案要求 |
|---|---|---|---|
| 平台默认域名（推荐） | `xxx.edgeone.app` | 国内直连、免备案、免费 | 无需 |
| 免费自定义域名 | `xxx.dnspod.cn` | 永久免费、HTTPS 自动 | 无需 |
| 付费注册域名 | `xxx.xyz` / `xxx.store` | 品牌自主、专业形象 | **需 ICP 备案** |

**操作步骤：**
1. **默认域名**：部署完成后平台自动分配（EdgeOne Pages 的 `*.edgeone.app`、FC 的 `*.fc.aliyuncs.com`），开箱即用，课程演示直接用这个即可。
2. **自定义域名（可选）**：
   - 在域名服务商（阿里云万网 / DNSPod / 腾讯云等）注册或复用已有域名。
   - 在 EdgeOne / FC 控制台「自定义域名」里添加，按提示做 **CNAME 解析**。
   - HTTPS 默认自动签发，无需手动配证书。
3. **ICP 备案提示**：若自定义域名**解析到国内节点**且用于对外服务，按工信部要求需完成 ICP 备案（约 5–20 个工作日）。**课程作业建议优先使用平台默认域名**，省去备案等待，聚焦部署流程本身。

---

## 5. SQLite 持久化说明（重要）

Recall 用本地 SQLite 文件存数据，但 Serverless（FC）实例的文件系统是**临时/按实例隔离**的：
- 函数实例运行期间可写，但**实例回收或扩缩容后数据可能丢失**（冷启动重置）。
- **课程演示 / MVP**：直接接受「演示数据可重置」，无需额外配置，最简单。
- **FC 代码目录是只读的**：本项目已通过环境变量把数据目录指向 `/tmp`（见 3.2 方式二的环境变量 `RECALL_DATA_DIR` / `RECALL_DB_PATH` / `RECALL_UPLOAD_DIR`），因此无需挂 NAS 也能正常写入；只是 `/tmp` 同样随实例回收而重置，演示数据仍可重置。
- **需要持久化**（进阶，按需求选）：
  - **挂载 NAS**：FC 可挂载阿里云 NAS 文件存储到 `/mnt/auto`，把 `backend/data` 指向挂载目录，SQLite 即持久。
  - **换云数据库**：把 SQLAlchemy 连接从 SQLite 改为阿里云 RDS（MySQL），数据天然持久、可多实例共享（需少量改 `backend/app/db/` 的连接串）。

---

## 6. 部署后验证清单
- [ ] 打开页面能看到 Recall 错题本 UI（非白屏）。
- [ ] 录入一道错题 → 列表出现 → 数据看板有图。
- [ ] AI 语音讲解 / 考前冲刺等 M7 功能可点击（Mock 模式返回确定性结果）。
- [ ] 后端 `/api/health` 或任意接口返回 200（可选，用于排查）。
- [ ] 访问域名是国内可直接打开、HTTPS 正常。

---

## 7. 常见问题
- **白屏 / `/api` 404**：分离部署时前端没指到后端。检查 `VITE_API_BASE` 或 EdgeOne 反向代理是否配置。
- **跨域报错 CORS**：方式 B 下需在后端 `allow_origins` 加入前端域名。
- **FC 端口不对**：容器方案监听端口需与 Dockerfile 中 uvicorn 端口一致（8000 或 9000）。
- **构建失败**：确认 `frontend/` 下有 `node_modules` 或让平台自动 `npm install`；本项目已含 `frontend/src/vite-env.d.ts` 保证 `import.meta.env` 类型正确。

---

## 8. 本地准备环境踩坑（venv Python 版本错配）

本项目的打包脚本 `deploy/build_fc_package.py` 会先在本地构建前端、再 import 后端，因此**本地 venv 必须能正常加载 `pydantic`**。常见报错：

```
ModuleNotFoundError: No module named 'pydantic_core._pydantic_core'
```

- **根因**：虚拟环境是 Python 3.12，但装依赖时用了系统默认的 `pip`。你的机器上 `python`/`pip` 默认**可能指向 WorkBuddy 托管的 Python 3.13**，于是 `pydantic_core` 被装成了 `cp313` 的编译二进制，3.12 加载不了。
- **正确做法**：装依赖前**先激活 venv**，并全程用 `python -m pip`（激活后 `python` 就是 venv 的 3.12）：

  ```powershell
  cd backend
  .\venv\Scripts\Activate.ps1
  python -m pip install -r requirements.txt
  ```

- **已错配的修复**：重建 venv（在**普通终端**执行；WorkBuddy 沙箱内因安全策略会拦截部分文件删除/覆盖）：

  ```powershell
  cd backend
  Remove-Item -Recurse -Force venv
  python -m venv venv
  .\venv\Scripts\Activate.ps1
  python -m pip install -r requirements.txt
  ```

> 本指南根据 Recall 实际架构（Python 单体 + 轻量依赖 + 前端 dist 内嵌托管）对原课程方案做了适配修正，确保步骤可真实落地。
